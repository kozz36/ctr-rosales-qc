# Sensitive: real QC data (RUCs, company data, SUNAT docs)
*.pdf
.playwright-mcp/
*.png
claude-prompt-*.md
*chat-export*

# Local SA-5 / vision-enabled run infra (env-specific — never commit)
/docker-compose.app.override.yml
/docs/playwright/*.txt
/sa5-*.md

# Local vision quantity-accuracy eval (#40) — real QC data, env-specific
/docs/eval/

# Local agent tooling (excluded from public repo)
.claude/
.atl/
.pi/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
backend/runs/

# Node / Vue
node_modules/
dist/
.vite/
*.tsbuildinfo
npm-debug.log*

# Secrets / local config
.env
.env.*
!.env.example
config.local.yaml
backend/config.local.yaml

# OS / IDE
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# stray local-command transcript dumps (never commit)
2026-*-local-command-*.txt

# KDE Dolphin folder metadata
.directory
